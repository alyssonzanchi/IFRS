#include <stdio.h>

float main() {
    float h, sexo;
    printf("Informe a sua altura e o seu sexo (sendo 1 para feminino e 2 para masculino): ");
    scanf("%f, %f", &sexo, &h);
    if(sexo = 1) {
        printf("O seu peso ideal e ", (72,7 * h) - 58);
    } else {
        printf("O seu peso ideal eh ", (62,1 * h) - 44,7);
    }
}